package sweProject;

import javax.swing.JOptionPane;

public class RealImage implements Image{
	   private String fileName;
	   boolean alreadyPresent = false;

	   public RealImage(String fileName){
	      this.fileName = fileName;
	      loadFromDisk(fileName);
	   }

	   @Override
	   public void display() {
		   if(!alreadyPresent ) {
			   JOptionPane.showInputDialog("Displaying from fresh load" + fileName , null);
		   } else {
			   JOptionPane.showInputDialog("Displaying from existing loaded image" + fileName , null);
		   }
	   }

	   private void loadFromDisk(String fileName){
		   JOptionPane.showInputDialog("Loading " + fileName + " for the first time", null);
		   alreadyPresent = true;
	   }
}
